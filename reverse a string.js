const arr = [1, 2, 3, 4, 5];

arr.reverse(); // This will reverse the array in place

// const arr1 = arr.join(" "); // Join the reversed array elements into a string separated by a space

// split() method, which splits the string into an array of individual characters.

console.log(arr);